export interface IEntrada {
    // esto, para mí, no es una interface clásica, es una class clásica ...
    userId?: number;
    id: number;
    title: string;
    body: string;
    autor: string;
    fecha: string;
}
